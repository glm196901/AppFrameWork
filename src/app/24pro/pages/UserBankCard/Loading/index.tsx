import React from 'react';
import ContentLoader from 'react-content-loader';

export default () => (
  <ContentLoader
    height={160}
    // width={300}
    speed={2}
    primaryColor="#f3f3f3"
    secondaryColor="#ecebeb"
  ></ContentLoader>
);
